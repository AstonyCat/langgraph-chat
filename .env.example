# Set your API key to use a real LLM (Zhipu AI GLM by default).
# Without this, the app runs in echo/demo mode.
# OPENAI_API_KEY=your-zhipu-api-key

# Base URL defaults to Zhipu AI. Override for other OpenAI-compatible APIs.
# OPENAI_API_BASE=https://open.bigmodel.cn/api/paas/v4

# Model defaults to glm-4-flash. Other options: glm-4-flash, glm-4-air, etc.
# OPENAI_MODEL=glm-4-flash
